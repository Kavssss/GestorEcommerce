package frontend.views.utils;

import javafx.scene.control.Alert;
import javafx.scene.control.Alert.AlertType;
import javafx.scene.control.Button;
import javafx.scene.control.ButtonType;

public class Alerts {
	
	public static void showAlert(String title, String header, String content, AlertType type) {
		Alert alert = new Alert(type);
		alert.setTitle(title);
		alert.setHeaderText(header);
		alert.setContentText(content);
		alert.show();
	}
	
	public static Boolean confirmationAlert(String title, String header, String content) {
		final Boolean retorno = Boolean.FALSE;
		Alert alert = new Alert(AlertType.INFORMATION);
		alert.setTitle(title);
		alert.setHeaderText(header);
		alert.setContentText(content);
		
		ButtonType btnSim = new ButtonType("Sim");
		ButtonType btnNao = new ButtonType("Não");
		
		alert.getButtonTypes().set(0, btnSim);
		alert.getButtonTypes().set(1, btnNao);
        Button confirmation = (Button) alert.getDialogPane().lookupButton(btnSim);
        alert.showAndWait();
        
        confirmation.setOnAction(e -> {
            retorno = Boolean.TRUE;
        });		
        
        return retorno;
	}
	
}
